block variables
- let path = self.htmlWebpackPlugin.options

doctype html
html(lang="en")
    include ../organisms/head
    head
        +head(path.file)
        block head
    body.d-flex.flex-column
        block main
        block scripts
